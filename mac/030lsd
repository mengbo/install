#!/bin/sh -v

brew tap homebrew/cask-fonts
brew install font-meslo-lg-nerd-font

brew install lsd

mkdir -p ~/.zshrc.d
cat >> ~/.zshrc.d/90lsd.zsh << EOF
lsd -d . &>/dev/null && alias ls='lsd'
EOF

brew install vivid

mkdir -p ~/.profile.d
cat > ~/.profile.d/vivid.sh << EOF
export LS_COLORS="$(vivid generate solarized-dark)"
EOF
